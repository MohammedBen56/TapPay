# telemetry

Python FastAPI + Chart.js bump-correlation harness. Built in Phase 2 (Milestone 0):
streams both phones' raw IMU/magnetometer/BLE RSSI to a WebSocket dashboard for the
bump-correlation spike. Runs in Docker; no host Python install needed.
